#include "lazy/utils.hpp"
template <typename T>
struct Node
{
    T value;
    struct Node* parent;
    struct Node* left;
    struct Node* right;
};


int main(int argc, char const *argv[])
{
    
    return 0;
}